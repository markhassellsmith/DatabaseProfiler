For an application called IRIS, the manager has asked a team of 3 to provide documentation of the SQL Server database schema and data profile.  
I want to use tools such as SSMS, Visual Studio, SSDT projects, Copilot, etc. to discover, explore, profile, and document.  
Of course we want things in the data profile such as min, max, lists of unique values, outliers, frequency counts where relevant, etc.  
If we do all this work, it seems like we could have an application that connects to a database and presents the schema and the data profile 
in a very consumable way, and not just a huge spreadsheet (although I'm sure it's wanted in a file report).  
Give me some general thoughts first, not too much detail.